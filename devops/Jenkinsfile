pipeline { 
    agent any 
    environment { 
        INSTAGRAM_NERIO = '@Nerio' 
    }
    tools {
        maven 'Maven'
    }

    stages {
        stage('check env') {
            steps {
                sh "mvn -v"
            }
        }
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
    } 
} 