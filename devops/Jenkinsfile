pipeline { 
    agent any 
    environment { 
        INSTAGRAM_NERIO = '@Nerio' 
    }
    tools {
        jdk 'Java17'
        maven 'Maven'
    }
    options {
        skipDefaultCheckout(true)
    }

    stages {
        stage('check env') {
            steps {
                sh "mvn -v"
                sh "java -version"
            }
        }
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
    } 
} 